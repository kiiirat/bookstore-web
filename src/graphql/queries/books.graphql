query PaginatedBooks($page: Float!, $take: Float!) {
  paginatedBooks(page: $page, take: $take) {
    list {
      id
      title
      author
      price
      imageUrl
      stock
      createdAt
      updatedAt
    }
    pages
  }
}
